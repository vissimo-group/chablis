machine:
  node:
    version: 8.9.3
  services:
    - docker
  environment:
    TRAVIS_REPO_SLUG: Evino/chablis

dependencies:
  pre:
    - npm install

test:
  override:
    - npm run lint
    - npm run test

deployment:
    production:
      branch: master
      commands:
        - npm run build
        #doc
        #dist
        #publish